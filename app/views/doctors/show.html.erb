
<h1><%=@doctor.name%></h1>
<h3> <%=@doctor.department%> </h3>

<ul>
  <% @doctor.appointments.each do |app| %>
    <li>
      <%= app.appointment_datetime.strftime('%B %d, %Y at %T') %> <%= link_to app.patient.name, patient_path(app.patient) %>
    </li>
    <% end %>
</ul>
