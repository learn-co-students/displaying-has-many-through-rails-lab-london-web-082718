<h1>Dr <%= @doctor.name %></h1>
<h2>Appointments</h2>
<ul>
<% @doctor.appointments.each do |apt| %>
<li> <%= apt.appointment_datetime.strftime('%B %d, %Y at %T') %>, <%= link_to apt.patient.name, apt.patient %> </li>
<% end %>
